#!/bin/sh

if which rbenv > /dev/null; then
  if which kvizer > /dev/null; then
    cd $(which kvizer | sed "s#\(.*\)/.*#\1#")
  else
    cd $(pwd -P)/bin
  fi
  rbenv exec ruby ./katello-virtualizer.rb $@
else
  echo 'no rbenv, implement it for your tool'
fi
